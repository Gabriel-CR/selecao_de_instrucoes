>>>>>>>>
q3
MOVE(MEM(+(MEM(+(FP,CONST a)),*(TEMP i,CONST 4))),MEM(+(FP,CONST X)))
========
LOAD 	r1 <- M[FP + a]
ADDI 	r2 <- r0 + 4
MUL 	r2 <- ri * r2
ADD 	r1 <- r1 + r2
ADDI 	r2 <- FP + X
MOVEM 	M[r1] <- M[r2]
<<<<<<<<